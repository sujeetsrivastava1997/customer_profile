name: Customer Profile CI/CD Pipeline

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:

  # 1️⃣ Build & Unit Tests
  build:
    name: Build & Unit Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Maven Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('customer-profile-search-app/pom.xml') }}

      - name: Build Project
        working-directory: customer-profile-search-app
        run: mvn clean package --batch-mode

      - name: List target folder (debug)
        working-directory: customer-profile-search-app
        run: ls -la target

      - name: Run Unit Tests
        working-directory: customer-profile-search-app
        run: mvn test

  # 2️⃣ API Tests
  api-tests:
    name: API Tests
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      - name: Run API Tests
        working-directory: customer-profile-search-app
        run: mvn verify

  # 3️⃣ Docker Build
  docker-build:
    name: Docker Build
    needs: api-tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t customer-profile-search-app:latest ./customer-profile-search-app

      - name: Save Docker Image as Artifact
        run: docker save customer-profile-search-app:latest -o customer-profile-search-app.tar

      - name: Upload Docker Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: customer-profile-search-app.tar

  # 4️⃣ Deploy to Staging
  deploy-staging:
    name: Deploy to Staging
    needs: docker-build
    runs-on: ubuntu-latest

    steps:
      - name: Download Docker Image Artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image
          path: .

      - name: Load Docker Image
        run: docker load -i customer-profile-search-app.tar

      - name: Run Docker Container
        run: |
          docker stop demo-app || true
          docker rm demo-app || true
          docker run -d -p 8080:8080 --name demo-app customer-profile-search-app:latest

      - name: Wait for App to be Ready
        run: |
          echo "Waiting for the app to be ready..."
          for i in {1..30}; do
            if curl -s -f http://localhost:8080/api/customers; then
              echo "App is ready!"
              exit 0
            fi
            echo "Waiting 2s..."
            sleep 2
          done
          echo "App failed to start in time"
          docker logs demo-app
          exit 1


  # 5️⃣ Selenium UI Tests (Mandatory)
  ui-tests:
    name: Selenium UI Automation
    needs: deploy-staging
    runs-on: ubuntu-latest

    services:
      selenium:
        image: selenium/standalone-chrome:latest
        ports:
          - 4444:4444

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      - name: Run Selenium Tests
        env:
          BASE_URL: http://localhost:8080
          SELENIUM_URL: http://localhost:4444/wd/hub
        working-directory: customer-profile-search-app
        run: mvn test -P ui

  # 6️⃣ Pipeline Success Confirmation
  pipeline-success:
    name: Pipeline Completed Successfully
    needs: ui-tests
    runs-on: ubuntu-latest
    steps:
      - name: Done
        run: echo "CI/CD Pipeline executed successfully 🚀"
